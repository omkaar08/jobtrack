spring.application.name=jobtrack
server.port=8080

# Database Configuration (MySQL)
# Default database: jobtrack
# Note: createDatabaseIfNotExist=true automatically creates the jobtrack DB if it doesn't exist.
spring.datasource.url=jdbc:mysql://localhost:3306/jobtrack?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Omkar@123
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA / Hibernate Settings
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

